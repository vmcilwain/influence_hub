<p style="color: green"><%= notice %></p>

<%= render @task %>

<div>
  <%= link_to "Edit this task", edit_campaign_task_path(@campaign, @task) %> |
  <%= link_to "Back to tasks", campaign_tasks_path(@campaign) %>

  <%= button_to "Destroy this task", campaign_task_path(@campaign, @task), method: :delete %>
</div>
